; generated by drom from package skeleton 'library'

(library
  (name ocamlup_lib)
  (public_name ocamlup_lib)
  (wrapped true)
  (libraries ocp-indent.lexer ocp-indent.lib ocp-indent.dynlink ez_file cmdliner unix )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package ocamlup_lib))


